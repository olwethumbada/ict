<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onward.ict">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home" />
        <activity android:name=".personalised" />
        <activity android:name=".forum" />
        <activity android:name=".topic" />
        <activity android:name=".Admin_home" />
        <activity android:name=".Admin_category" />
        <activity android:name=".add_category" />
        <activity
            android:name=".north_map"
            android:screenOrientation="portrait" />
        <activity android:name=".main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin_staff" />
        <activity
            android:name=".login_popup"
            android:theme="@style/AppTheme.PopMe" />
        <activity android:name=".comments" />
        <activity android:name=".emergency" />
        <activity android:name=".add_staff" />
        <activity android:name=".profile" />
        <activity android:name=".module" />
        <activity android:name=".module_details" />
    </application>

</manifest>